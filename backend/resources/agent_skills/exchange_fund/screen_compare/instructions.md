没有用户指定代码时，第一步必须调用 `discover_exchange_fund_candidates` 从结构化全市场快照发现候选，禁止凭记忆猜代码。主题核心产品与相关代理产品必须分开披露，代理产品的底层主题权重未经持仓证据核验时不得冒充纯主题 ETF。逐只调用产品、行情、历史、流动性和相对强弱工具时，严格使用返回的 `deep_analysis_shortlist`，最多深挖 4 只；其余候选只使用全市场快照说明排除理由，避免耗尽工具预算。完成数据准备后必须调用 `screen_compare_exchange_funds`：只有它返回 `ranking_is_formal=true` 才能输出正式排名；否则只能输出条件性比较和结构化缺口。正式排名必须具备标的核验、流动性和数据日期。缺失维度按零分且不重新归一化；评分仅作排序，不作为买入信号。
